/**
 * A for each loop like 'for (i in 1..3) { }'
 */

/*
export class ForInStatement {
    constructor(
        public readonly left: VariableReference,
        public readonly right: Expression,
        public readonly body: BlockStatement
    ) {}
}
*/
